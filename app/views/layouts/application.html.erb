<!DOCTYPE html>
<html>
  <head>
    <title>BookingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js" integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1" crossorigin="anonymous"></script>
    <link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
  </head>

  <body>
    <header>
      <nav class="navbar navbar-dark bg-dark">
        <%= link_to image_tag("logo.png" ), "/", class: "navbar-brand" %>
        <div class="d-none d-md-block mr-auto">
          <div class="nav-item active d-inline-block">
            <%= form_with url: rooms_search_path, method: :get do |f| %>
            <div class="header-search">
              <%= f.text_field :address, placeholder: "\uf3c5 エリア", class: "form-control rounded fa"%>
            </div>
          <% end %>
        </div>
        <div class="nav-item active d-inline-block">
          <%= form_with url: rooms_search_path, method: :get do |f| %>
          <div class="header-search">
            <%= f.text_field :keyword, placeholder: "\uf002 キーワード", class: "form-control rounded fa"%>
          </div>
        <% end %>
      </div>
    </div>
    <% if session[:user_id] %>
      <div class="user-name text-white">
        <%= @current_user.name %>
      </div>
      <div class="dropdown">
        <button class="btn dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= image_tag @current_user.image, class: 'user_picture rounded-circle' if @current_user.image.attached? %>
        </button>

        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
          <p class="dropdown-item">
            <%= link_to "ルーム登録", new_room_path, :style=>"color:black;" %>
          </p>
          <p class="dropdown-item">
            <%= link_to "予約済みルーム一覧", books_index_path, :style=>"color:black;" %>
          </p>
          <p class="dropdown-item">
            <%= link_to "登録済みルーム一覧", rooms_path, :style=>"color:black;" %>
          </p>
          <p class="dropdown-item">
            <%= link_to "設定", users_account_path, :style=>"color:black;" %>
          </p>
          <p class="dropdown-item">
            <%= link_to "ログアウト", logout_path, method: :post, :style=>"color:black;" %>
          </p>
        </div>
      </div>
    <% else %>
      <button class="btn btn-primary mx-1" type="button">
        <%= link_to "ログイン", login_path, method: :get, :style=>"color:white;" %>
      </button>
      <button class="btn btn-secondary mx-1" type="button">
        <%= link_to "登録する", new_user_path, method: :get, :style=>"color:white;" %>
      </button>
    <% end %>
  </nav>
</header>
<body>
  <% flash.each do |key, value| %>
    <% key = "info" if key == "notice" %>
    <% key = "danger" if key == "alert" %>
    <%= content_tag :div, value, class:"text-center mb-0 alert alert-#{key}" %>
  <% end %>
  <%= yield %>
</body>
<footer class="bg-dark">
  <div class="container">
    <div class="h-100 text-white d-flex align-items-center justify-content-center">
      <p>© 2020 POTEPAN.INC</p>
    </div>
  </div>
</footer>
</body>
</html>
